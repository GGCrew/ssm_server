<%=
	{
		:server_path => "/photos/eye-fi/",
		:camera_folder => "#{@photo.camera_folder}",
		:date_folder => "#{@photo.date_folder}",
		:filename => "#{@photo.filename}",
		:full_path => "/photos/eye-fi/#{@photo.full_path}",
	}.to_json.html_safe
-%>