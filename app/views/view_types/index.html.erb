<h1>Listing view_types</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @view_types.each do |view_type| %>
  <tr>
    <td><%= link_to 'Show', view_type %></td>
    <td><%= link_to 'Edit', edit_view_type_path(view_type) %></td>
    <td><%= link_to 'Destroy', view_type, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New View type', new_view_type_path %>
